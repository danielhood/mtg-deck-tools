# Dependency expansion — active

**Selected for immediate implementation.** Only items here should be in flight alongside [product active](../active.md).

Status as of 2026-06-04: **none selected.** Product roadmap priority is **UX7** (web UI). No dependency expansion row is committed until explicitly promoted from [backlog.md](backlog.md).

## Optional (not selected)

| Item | Notes |
| --- | --- |
| Priority 7 remainder | Golden fixtures, broader GY patterns, recursion post-fill package — see [backlog.md](backlog.md) |

When promoting work: move the row from backlog → this file, then implement per [dependency-validation.md](../../sdlc/dependency-validation.md).

## Regression (always)

`mtg-deck-tools analyze run --fail-on-expect` — **30/30** after dependency changes ([deck-analysis.md](../../specs/deck-analysis.md)).
